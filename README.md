# Learning-Spring-boot

Notes and projects from learning Spring Boot.

## Projects

- [`Hello-and-api/`](./Hello-and-api/) — first Spring Boot app: `@RestController`, JSON sending/receiving, `@GetMapping` / `@PostMapping`.
